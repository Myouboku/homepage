<html>

<style>
  html {
    color-scheme: dark;
  }

  /* TODO faire une longueur maximum,
  si dépassée -> défilement vertical */
  .flex-container {
    margin-top: 10em;
    border-top: 1px solid;
    border-bottom: 1px solid;
    display: flex;
    justify-content: center;
    width: 40em;
    margin-left: auto;
    margin-right: auto;
  }

  .table {
    border-left: 1px solid;
    border-right: 1px solid;
    width: 100%;
  }

  .head-cell {
    text-align: center;
    border-bottom: 1px solid;
  }

  .cell {
    padding: 0.3em;
    margin-left: 0.2em;
  }

  a:link,
  a:visited {
    text-decoration: none;
    color: coral;
    transition-property: color;
    transition-duration: 500ms;
  }

  a:hover {
    text-decoration: none;
    color: aquamarine;
  }

  a:active {
    text-decoration: none;
    color: coral;
  }
</style>

<body>
  {{!-- TODO: ajouter possibilité de modifier les bookmarks dans json --}}
  {{!-- TODO: possibilité d'ajouter de nouveaux bookmarks et nouvelles catégories --}}
  {{!-- TODO: voir si instaurer une limite de catégories ou un défilement horizontal --}}
  <div class="flex-container">
    {{#each data}}
    <div class="table">
      <div class="row">
        <div class="head-cell"><b>{{@key}}</b></div>
      </div>
      {{#each this}}
      <div class="row">
        <a href='{{this}}'>
          <div class="cell">{{@key}}</div>
        </a>
      </div>
      {{/each}}
    </div>
    {{/each}}
  </div>
</body>

</html>